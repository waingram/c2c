= simple_form_for [@package, @item], :html => { :class => 'form-horizontal' } do |f|
  = f.input :description
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), package_items_path(@package), :class => 'btn'
