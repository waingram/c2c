- model_class = Item
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:description)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @items.each do |item|
      %tr
        %td= link_to item.id, package_item_path(@package, item)
        %td= item.description
        %td= item.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_package_item_path(@package, item), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), package_item_path(@package, item), :method => :delete, :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')), :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_package_item_path, :class => 'btn btn-primary'
