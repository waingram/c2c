- model_class = Package
.page-header
  %h1 Create a new upload package
%table.table.table-striped
  -#%thead
  -#  %tr
  -#    %th= model_class.human_attribute_name(:id)
  -#    %th= model_class.human_attribute_name(:title)
  -#    %th= model_class.human_attribute_name(:description)
  -#    %th= model_class.human_attribute_name(:created_at)
  -#    %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @packages.each do |package|
      %tr
        %td= link_to package.id, package_path(package)
        %td= package.title
        %td= package.description
        %td=l package.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_package_path(package), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), package_path(package), :method => :delete, :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')), :class => 'btn btn-mini btn-danger'

= link_to "Create New", new_package_path, :class => 'btn btn-primary'
