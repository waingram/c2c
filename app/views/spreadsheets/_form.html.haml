= simple_form_for @spreadsheet, :html => { :class => 'form-horizontal' } do |f|
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), spreadsheets_path, :class => 'btn'
